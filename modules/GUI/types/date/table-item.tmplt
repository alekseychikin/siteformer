{ if (value) }
  { if (str_len(value.date)) }
    { date = str_split(value.date, '-') }
    { time = str_split(value.time, ':') }
    { date[2] }.{ date[1] }.{ date[0] }
    { if (field.settings.useTime) }
      { time[0] }:{ time[1] }
    { endif }
  { endif }
{ endif }
